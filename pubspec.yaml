name: fuel_tracker_app
description: "Fuel Tracking & Driving Behavior Analysis App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.0.5
  google_fonts: ^6.1.0
  geolocator: ^13.0.2
  google_maps_flutter: ^2.5.0
  fl_chart: ^0.69.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true